/** The reorderable blocks on the home page, in the order they are stored. */
export enum HomeSection {
  SUBSCRIPTIONS = 'subscriptions',
  DOWNLOADS = 'downloads',
}

export type HomeSectionOrder = HomeSection[];
